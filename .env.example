BASE_URL=http://54.38.75.50/FocusReg.DEV.V4
LOGIN_PATH=/app-dashboard/dashboard-configuration

# App shell route (drawer menu). Dashboard Configuration tests open the menu from here.
APP_HOME_PATH=/app-dashboard/dashboard
USERNAME=your.username@focusync.local.uk
PASSWORD=your_password_here

# Optional: if the Windows popup needs DIFFERENT credentials than the app login form
# HTTP_AUTH_USERNAME=
# HTTP_AUTH_PASSWORD=

# If your login page uses a dedicated sign-in route instead of a protected page,
# set LOGIN_PATH accordingly (e.g. /Account/Login).
